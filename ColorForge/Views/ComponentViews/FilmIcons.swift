//
//  FilmIcons.swift
//  ColorForge
//
//  Created by Ben Quinton on 09/08/2025.
//

import Foundation
import SwiftUI



struct KodakPortra: View {
    var body: some View {
        ZStack {
            Image(systemName: "square.fill")
                .resizable()
                .aspectRatio(contentMode: .fit)
                .foregroundStyle(Color("kodakYellow"))
                .frame(width: 25, height: 25)
                .foregroundColor(Color("SideBarText"))
            
            Image(systemName: "square.tophalf.filled")
                .resizable()
                .aspectRatio(contentMode: .fit)
                .foregroundStyle(Color("kodakPortra"))
                .frame(width: 25, height: 25)
                .foregroundColor(Color("SideBarText"))
            
            Image(systemName: "square")
                .resizable()
                .aspectRatio(contentMode: .fit)
                .foregroundStyle(Color("SideBarText"))
                .frame(width: 26, height: 26)
                .foregroundColor(Color("SideBarText"))
        }
    }
}


struct KodakTMax: View {
    var body: some View {
        ZStack {
            Image(systemName: "square.fill")
                .resizable()
                .aspectRatio(contentMode: .fit)
                .foregroundStyle(Color("kodakYellow"))
                .frame(width: 25, height: 25)
                .foregroundColor(Color("SideBarText"))
            
            Image(systemName: "square.tophalf.filled")
                .resizable()
                .aspectRatio(contentMode: .fit)
                .foregroundStyle(Color("kodakBlack"))
                .frame(width: 25, height: 25)
                .foregroundColor(Color("SideBarText"))
            
            Image(systemName: "square")
                .resizable()
                .aspectRatio(contentMode: .fit)
                .foregroundStyle(Color("SideBarText"))
                .frame(width: 26, height: 26)
                .foregroundColor(Color("SideBarText"))
        }
    }
}


struct KodakTrix: View {
    var body: some View {
        ZStack {
            Image(systemName: "square.fill")
                .resizable()
                .aspectRatio(contentMode: .fit)
                .foregroundStyle(Color("kodakBlack"))
                .frame(width: 25, height: 25)
                .foregroundColor(Color("SideBarText"))
            
            Image(systemName: "square.tophalf.filled")
                .resizable()
                .aspectRatio(contentMode: .fit)
                .foregroundStyle(Color("kodakYellow"))
                .frame(width: 25, height: 25)
                .foregroundColor(Color("SideBarText"))
            
            Image(systemName: "square")
                .resizable()
                .aspectRatio(contentMode: .fit)
                .foregroundStyle(Color("SideBarText"))
                .frame(width: 26, height: 26)
                .foregroundColor(Color("SideBarText"))
        }
    }
}


struct KodakGold: View {
    var body: some View {
        ZStack {
            Image(systemName: "square.fill")
                .resizable()
                .aspectRatio(contentMode: .fit)
                .foregroundStyle(Color("kodakGold"))
                .frame(width: 25, height: 25)
                .foregroundColor(Color("SideBarText"))
            
            Image(systemName: "square.tophalf.filled")
                .resizable()
                .aspectRatio(contentMode: .fit)
                .foregroundStyle(Color("kodakYellow"))
                .frame(width: 25, height: 25)
                .foregroundColor(Color("SideBarText"))
            
            Image(systemName: "square")
                .resizable()
                .aspectRatio(contentMode: .fit)
                .foregroundStyle(Color("SideBarText"))
                .frame(width: 26, height: 26)
                .foregroundColor(Color("SideBarText"))
        }
    }
}


struct KodakE100: View {
    var body: some View {
        ZStack {
            Image(systemName: "square.fill")
                .resizable()
                .aspectRatio(contentMode: .fit)
                .foregroundStyle(Color("kodakYellow"))
                .frame(width: 25, height: 25)
                .foregroundColor(Color("SideBarText"))
            
            Image(systemName: "square.tophalf.filled")
                .resizable()
                .aspectRatio(contentMode: .fit)
                .foregroundStyle(Color("kodakBlue"))
                .frame(width: 25, height: 25)
                .foregroundColor(Color("SideBarText"))
            
            Image(systemName: "square")
                .resizable()
                .aspectRatio(contentMode: .fit)
                .foregroundStyle(Color("SideBarText"))
                .frame(width: 26, height: 26)
                .foregroundColor(Color("SideBarText"))
        }
    }
}





struct FujiProvia: View {
    var body: some View {
        ZStack {
            Image(systemName: "square.fill")
                .resizable()
                .aspectRatio(contentMode: .fit)
                .foregroundStyle(Color("fujiBlue"))
                .frame(width: 26, height: 26)
                .foregroundColor(Color("SideBarText"))
            
            Image(systemName: "square.tophalf.filled")
                .resizable()
                .aspectRatio(contentMode: .fit)
                .foregroundStyle(Color("fujiGreen"))
                .frame(width: 25, height: 25)
                .foregroundColor(Color("SideBarText"))
            
            Image(systemName: "square")
                .resizable()
                .aspectRatio(contentMode: .fit)
                .foregroundStyle(Color("SideBarText"))
                .frame(width: 26, height: 26)
                .foregroundColor(Color("SideBarText"))
        }
    }
}




struct FujiVelvia: View {
    var body: some View {
        ZStack {
            Image(systemName: "square.fill")
                .resizable()
                .aspectRatio(contentMode: .fit)
                .foregroundStyle(Color("fujiVelvia"))
                .frame(width: 26, height: 26)
                .foregroundColor(Color("SideBarText"))
            
            Image(systemName: "square.tophalf.filled")
                .resizable()
                .aspectRatio(contentMode: .fit)
                .foregroundStyle(Color("fujiGreen"))
                .frame(width: 25, height: 25)
                .foregroundColor(Color("SideBarText"))
            
            Image(systemName: "square")
                .resizable()
                .aspectRatio(contentMode: .fit)
                .foregroundStyle(Color("SideBarText"))
                .frame(width: 26, height: 26)
                .foregroundColor(Color("SideBarText"))
        }
    }
}
